{
  "label": "providers.openai-chat-request",
  "source": "plano-fase1-mvp.md §3 T6 — corpo chat.completions (stream:true + tools) montado por buildChatBody",
  "description": "Corpo do POST /v1/chat/completions do adapter OpenAI: system como primeira mensagem, tools (schema function), temperature e max_tokens opcionais. Contrato de saída do adapter (T6), usado por T7/T8.",
  "request": {
    "model": "chat-completions-test",
    "system": "Você é o OpenBot — assistente local.",
    "messages": [
      { "role": "user", "content": "Liste os arquivos do diretório atual." }
    ],
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "shell",
          "description": "Executa um comando no shell",
          "parameters": { "type": "object", "properties": { "cmd": { "type": "string" } }, "required": ["cmd"] }
        }
      }
    ],
    "temperature": 0.2,
    "maxTokens": 512
  },
  "expectedBody": {
    "model": "chat-completions-test",
    "messages": [
      { "role": "system", "content": "Você é o OpenBot — assistente local." },
      { "role": "user", "content": "Liste os arquivos do diretório atual." }
    ],
    "stream": true,
    "stream_options": { "include_usage": true },
    "tools": [
      {
        "type": "function",
        "function": {
          "name": "shell",
          "description": "Executa um comando no shell",
          "parameters": { "type": "object", "properties": { "cmd": { "type": "string" } }, "required": ["cmd"] }
        }
      }
    ],
    "temperature": 0.2,
    "max_tokens": 512
  }
}

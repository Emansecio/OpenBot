# T10 Fixes Implementation Plan

> **Status atual (2026-08-12): documento histórico.** Este documento preserva o plano/design original. O estado executável atual está em [`../../phase-2-local-execution-progress.md`](../../phase-2-local-execution-progress.md) e no [`README`](../../../README.md).


> **Uso deste plano:** consulte apenas as etapas pertinentes ao pedido vigente. Procedimentos, comandos e checkboxes registram o planejamento desta revisão; não autorizam execução, delegação, commit ou deploy. Siga o AGENTS.md atual e verifique o estado implementado antes de retomar.

**Goal:** Corrigir o bootstrap, o estado de `/health`, a validação de `clientNonce` e tornar o dedupe compatível com persistência futura.

**Architecture:** O `TurnRunner` usará o `TranscriptStore` como única fonte do ledger de nonces. O bootstrap registrará a mesa RPC e compartilhará o status do runner com o gateway. A implementação em memória preservará a API atual e a política FIFO.

**Tech Stack:** TypeScript, Node.js, Vitest, HTTP/SSE local.

---

### Task 1: Cobrir regressões de bootstrap e validação

**Files:**
- Modify: `test/rpc-send-gateway.integration.test.ts`
- Modify: `test/rpc-send.test.ts`

- [ ] Adicionar teste que sobe `startServer(0)` sem registro manual e confirma `POST /api/sendPrompt` retorna envelope de aceitação.
- [ ] Adicionar teste que usa provider bloqueado, consulta `/health` enquanto o turno está pendente e confirma `isBusy`/`activeAgentId`; liberar o provider e confirmar o estado inativo após `flush`.
- [ ] Adicionar casos de nonce `""`, whitespace, `null` e número esperando `RpcError`/HTTP 400.
- [ ] Rodar os testes focados e confirmar falha causada pelas lacunas existentes.

### Task 2: Estender o store com ledger de nonces

**Files:**
- Modify: `src/rpc/send.ts`
- Modify: `test/rpc-send.test.ts`

- [ ] Acrescentar ao `TranscriptStore` os métodos `hasAcceptedNonce`, `rememberAcceptedNonce` e `trimAcceptedNonces`.
- [ ] Implementar os métodos no store em memória com `Set` por agente e FIFO.
- [ ] Substituir o `Map` privado do runner pelo store injetado.
- [ ] Validar `clientNonce` como string não vazia após `trim`, preservando o valor original.
- [ ] Rodar testes T10 e confirmar que o teste novo de ledger fica verde.

### Task 3: Conectar runner, gateway e bootstrap

**Files:**
- Modify: `src/rpc/send.ts`
- Modify: `src/rpc/index.ts`
- Modify: `src/main.ts`
- Modify: `test/rpc-send-gateway.integration.test.ts`

- [ ] Expor no runner uma forma de status observável e garantir que a fila marque o agente ativo antes de executar e remova-o ao terminar.
- [ ] Fazer `registerRpcHandlers` aceitar/retornar o runner sem quebrar os usos existentes.
- [ ] No `startServer`, criar o gateway, registrar os handlers RPC e fornecer ao gateway o status do runner.
- [ ] Remover o status fixo `{isBusy:false, activeAgentId:null}`.
- [ ] Rodar os testes focados e confirmar bootstrap, health e SSE verdes.

### Task 4: Verificação final

**Files:**
- No código adicional; revisar `docs/superpowers/specs/2026-08-11-t10-fixes-design.md`.

- [ ] Rodar `npm test` em `C:\SuperAgent\openbot`.
- [ ] Rodar `npm run build`.
- [ ] Fazer probe HTTP real do `startServer` sem registro manual.
- [ ] Conferir diff/arquivos alterados e atualizar README somente se o estado documentado estiver incorreto.
